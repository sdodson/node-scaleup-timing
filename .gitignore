# Kubeconfig files contain embedded client certificates and private keys
kubeconfig
*kubeconfig
cluster-bot-*.kubeconfig

# Temporary files (scripts, scratch work)
tmp/

# Claude session artifacts
" /"

# Raw artifacts (YAML, JSON, logs, SVGs) — may contain secrets
data/

# kubeconfigs for running 
